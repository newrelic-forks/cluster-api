apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: '${CLUSTER_NAME}'
  namespace: default
  labels:
    cni: "${CLUSTER_NAME}-crs-0"
spec:
  clusterNetwork:
    services:
      cidrBlocks: ['${DOCKER_SERVICE_CIDRS}']
    pods:
      cidrBlocks: ['${DOCKER_POD_CIDRS}']
    serviceDomain: '${DOCKER_SERVICE_DOMAIN}'
  topology:
    class: "quick-start"
    version: "${KUBERNETES_VERSION}"
    controlPlane:
      metadata: {}
<<<<<<< HEAD:test/e2e/data/infrastructure-docker/v1beta1/bases/cluster-with-topology.yaml
      nodeDrainTimeout: "30s"
      replicas: ${CONTROL_PLANE_MACHINE_COUNT}
    workers:
      machineDeployments:
      - class: "default-worker"
        name: "md-0"
        nodeDrainTimeout: "30s"
        replicas: ${WORKER_MACHINE_COUNT}
        failureDomain: fd4
    variables:
    - name: etcdImageTag
      # We set an empty value to use the default tag kubeadm init is using.
      value: ""
    - name: coreDNSImageTag
      # We set an empty value to use the default tag kubeadm init is using.
      value: ""
=======
      nodeDeletionTimeout: "30s"
      replicas: ${CONTROL_PLANE_MACHINE_COUNT}
    workers:
      machineDeployments:
        - class: "default-worker"
          name: "md-0"
          nodeDeletionTimeout: "30s"
          failureDomain: fd4
          metadata:
            annotations:
              cluster.x-k8s.io/cluster-api-autoscaler-node-group-max-size: "5"
              cluster.x-k8s.io/cluster-api-autoscaler-node-group-min-size: "2"
    variables:
      - name: etcdImageTag
        value: ""
      - name: coreDNSImageTag
        value: ""
      - name: preLoadImages
        value: ${DOCKER_PRELOAD_IMAGES:-[]}

>>>>>>> v1.5.7:test/e2e/data/infrastructure-docker/main/cluster-template-topology-autoscaler/cluster-autoscaler.yaml
